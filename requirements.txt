# Base runtime dependencies. Direct dependencies are pinned here; transitive
# versions are locked in constraints.txt for reproducible fresh installs.
-c constraints.txt

# Risk-bearing features (accounts, 2FA, REST API) are opt-in and add their own
# dependencies when you enable them; see the README.
Django==6.0.6
django-environ==0.14.0
psycopg2-binary==2.9.12
gunicorn==26.0.0
argon2-cffi==25.1.0
